pytest>=8.0
numpy>=1.26
fastapi>=0.110
uvicorn>=0.27

# Starlette's TestClient needs an HTTP client library, and which one depends on
# the starlette version that resolves: older releases want httpx, 0.53 and later
# want httpx2. Both are declared because this file does not pin fastapi, so both
# resolutions are reachable. CI found this; a local environment that already had
# one of them installed hid it completely.
httpx>=0.27
httpx2>=2.13
